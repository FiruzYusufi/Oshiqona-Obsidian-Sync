---
tags: [сервис, angular, шаблон]
created: <% tp.date.now("YYYY-MM-DD") %>
library: ""
status: planned
---

# Сервис: <% tp.file.title %>

## Обзор

**Библиотека**: `@core` / `@core/auth` / `@shared/cdk`  
**Путь**: `libs/.../`  
**Тип**: Injectable Service

## Назначение

> Что делает этот сервис?

## Методы

| Метод | Параметры | Возвращает | Описание |
|-------|----------|-----------|---------|
| `method1()` | `id: string` | `Observable<T>` | Описание |
| `method2()` | — | `Observable<T[]>` | Описание |

## API Endpoints

| Endpoint | Метод | Описание |
|----------|-------|---------|
| `/api/resource` | GET | Получить список |
| `/api/resource/:id` | GET | Получить по ID |
| `/api/resource` | POST | Создать |
| `/api/resource/:id` | PUT | Обновить |
| `/api/resource/:id` | DELETE | Удалить |

## Шаблон сервиса

```typescript
import { Injectable } from '@angular/core';
import { HttpClient } from '@angular/common/http';
import { Observable } from 'rxjs';

@Injectable({ providedIn: 'root' })
export class <% tp.file.title %>Service {
  constructor(private http: HttpClient) {}

  getAll(): Observable<any[]> {
    return this.http.get<any[]>('/api/resource');
  }

  getById(id: string): Observable<any> {
    return this.http.get<any>(`/api/resource/${id}`);
  }

  create(data: any): Observable<any> {
    return this.http.post<any>('/api/resource', data);
  }

  update(id: string, data: any): Observable<any> {
    return this.http.put<any>(`/api/resource/${id}`, data);
  }

  delete(id: string): Observable<void> {
    return this.http.delete<void>(`/api/resource/${id}`);
  }
}
```

## Экспорт

Добавить в `libs/.../src/index.ts`:
```typescript
export * from './lib/services/<% tp.file.title.toLowerCase() %>.service';
```

## Связанные документы

- [[04-Библиотеки]]
- [[05-Сервисы]]

---
**Создано**: <% tp.date.now("YYYY-MM-DD HH:mm") %>
