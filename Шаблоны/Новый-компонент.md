---
tags: [компонент, angular, шаблон]
created: <% tp.date.now("YYYY-MM-DD") %>
app: ""
path: ""
status: planned
---

# Компонент: <% tp.file.title %>

## Обзор

**Приложение**: Admin / Client / Shared  
**Путь**: `apps/.../`  
**Селектор**: `app-`  
**Тип**: Standalone Component

## Назначение

> Что делает этот компонент?

## Входные параметры (Inputs)

| Параметр | Тип | Обязательный | Описание |
|---------|-----|-------------|---------|
| `param1` | `string` | ✅ | Описание |
| `param2` | `number` | ❌ | Описание |

## Выходные события (Outputs)

| Событие | Тип | Описание |
|---------|-----|---------|
| `event1` | `EventEmitter<string>` | Описание |

## Используемые сервисы

- `ServiceName` из `@core`

## Шаблон компонента

```typescript
import { Component, Input, Output, EventEmitter, ChangeDetectionStrategy } from '@angular/core';

@Component({
  selector: 'app-<% tp.file.title.toLowerCase() %>',
  templateUrl: './<% tp.file.title.toLowerCase() %>.component.html',
  styleUrls: ['./<% tp.file.title.toLowerCase() %>.component.scss'],
  changeDetection: ChangeDetectionStrategy.OnPush,
  standalone: true,
})
export class <% tp.file.title %>Component {
  @Input() param1!: string;
  @Output() event1 = new EventEmitter<string>();
}
```

## Команда генерации

```bash
npx nx g @nx/angular:component pages/<% tp.file.title.toLowerCase() %> --project=oshiqona-chat
```

## Связанные документы

- [[03-Приложение-Client]]
- [[07-Компоненты]]

---
**Создано**: <% tp.date.now("YYYY-MM-DD HH:mm") %>
